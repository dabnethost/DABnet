#include "Structure.hpp"

namespace DABnetUtils {

    namespace DABformat {

        class StructureTools {

            public:

                static Structure::dabFile stringArrayToDabFile(std::string strings[]);

        };

    }
}